# Dagster instance configuration.
# DAGSTER_HOME must be set in the environment (see .env.example).
# Dagster will read this file from $DAGSTER_HOME/dagster.yaml.

storage:
  sqlite:
    base_dir:
      env: DAGSTER_HOME
